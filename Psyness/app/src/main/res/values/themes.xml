<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Psyness" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Psyness2" parent="Base.Theme.Psyness" >
        <item name="android:button">@color/morado</item>
        <item name="android:icon">@color/black</item>
        <item name="android:itemBackground">@color/black</item>
        <item name="colorPrimary">@color/morado</item>
        <item name="android:windowBackground">#FFFFFF</item>
        <item name="android:background">#FFFFFF</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:fontFamily">@font/nutito_black</item>

    </style>


</resources>